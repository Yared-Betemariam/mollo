export const authRoutes = ["/signin", "/auth/error"];

export const defaultLoginRedirect = "/editor";

export const publicRoutes = ["/"];

export const redirects = [{ from: "/admin", to: "/admin/users" }];
